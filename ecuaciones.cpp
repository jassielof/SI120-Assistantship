#include <iostream>

using namespace std;

// tipo linear Ax + B = C
float ecuacionLineal(float A, float B, float C) {
    return (C - B) / A;
}

// sistema de ecuaciones
// tipo:
// Ax + By = C
// Dx + Ey = F
float sistemaEcuaciones(
    float A,
    float B,
    float C,
    float D,
    float E,
    float F
) {
    
}

int main() {
    return 0;
}